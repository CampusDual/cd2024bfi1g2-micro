#ifndef CONFIG_H
#define CONFIG_H

// Configuración de Wi-Fi
extern const char* ssid;          // Declaración externa de la variable ssid
extern const char* password;      // Declaración externa de la variable password
extern const char* serverUrl;     // Declaración externa de la variable serverUrl

#endif // CONFIG_H